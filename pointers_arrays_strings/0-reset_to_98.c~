#include "main.h"
/**
 * reset_to_98 - writes the character c to stdout
 * @n: The integer we have to point
 *
 * Return: nothing.
 * On error, -1 is returned, and errno is set appropriately.
 */
void reset_to_98(int *n)
{
  *n = 98;
}
