#include "main.h"
/**
 * _abs - returns the absolute value of the number
 *@num: number
 * Return: the absolute value of the parameter.
 */
int _abs(int num)
{
if (num < 0)
return (-num);
else
return (num);
}
